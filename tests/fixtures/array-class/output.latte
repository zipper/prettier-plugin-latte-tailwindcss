<!-- Array class fixture: bare identifiers and dynamic barriers only.
     Quoted strings and keyed pairs break the HTML parser (see integration.test.ts). -->
<div class="{[mt-4," flex, block]}>bare identifiers</div>
<a class="{[mt-4," flex, block, hidden]}>multiple bare identifiers</a>
<p class="{[flex," $dynamicClass, hidden, block]}>dynamic barrier</p>
